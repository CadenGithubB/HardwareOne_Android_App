<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Window-level theme used before Compose takes over. Uses the framework Material
        theme (no com.google.android.material dependency) so the launch window is black
        and chrome-free; all real UI/colour comes from the Compose theme.
    -->
    <style name="Theme.HardwareOneConsole" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
