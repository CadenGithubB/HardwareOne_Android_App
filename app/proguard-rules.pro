# Minification is disabled (see app/build.gradle.kts). This file is kept so that
# enabling `isMinifyEnabled = true` later is a one-line change. No custom rules are
# needed: the app uses only the framework BLE APIs and Compose, both already covered
# by the default AGP/Compose consumer rules.
